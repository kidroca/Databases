Clean and Rebuild the solution to get the missing packages/dependencies. 
Build -> Clean Solution
Build -> Rebuild Solution

I don't know if my connection string will work on other computers, if there is a problem try to
create a new DbContext for the Northwind database with ADO.NET Entity Data Model